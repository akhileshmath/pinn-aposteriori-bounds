# Results Directory

This directory stores machine-readable outputs from the refactored pipeline.

Primary file:
- `validated_results.json` generated by `python experiments/run.py`

Auxiliary files:
- smoke-test outputs from targeted debugging runs
- legacy JSON files retained for provenance only

Do not treat legacy result files as part of the validated publication pipeline unless they are explicitly regenerated through `experiments/run.py`.
